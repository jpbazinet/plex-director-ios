# Cloud build for the Plex Director iOS shell. No Mac needed.
workflows:
  ios-testflight:
    name: iOS → TestFlight
    max_build_duration: 45
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Plex Director ASC   # the name you give the API key in Codemagic → Teams → Integrations
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.jpbazinet.plexdirector
      vars:
        BUNDLE_ID: com.jpbazinet.plexdirector
        XCODE_WORKSPACE: ios/App/App.xcworkspace
        XCODE_SCHEME: App
      node: 20
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install and create the native project
        script: |
          npm install
          npx cap add ios
          npx cap sync ios
          sh prepare-ios.sh
      - name: Set up signing
        script: |
          xcode-project use-profiles
      - name: Bump build number to match App Store Connect
        script: |
          cd ios/App
          LATEST=$(app-store-connect get-latest-testflight-build-number "$APP_STORE_APPLE_ID" || echo 0)
          agvtool new-version -all $((LATEST + 1))
      - name: Build the .ipa
        script: |
          xcode-project build-ipa --workspace "$XCODE_WORKSPACE" --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Internal
